# Frontend contribution rules

- Use feature-first folders and strict TypeScript.
- Keep route files focused on navigation and screen composition.
- Add loading, empty, error and offline states to new screens.
- Run `pnpm typecheck`, `pnpm lint` and `pnpm test` before a PR.
